# Team Model Swapping - Visual Guide 🎨

## 🔄 The Magic of Config-Driven Training

```
┌─────────────────────────────────────────────────────────────┐
│                    Team Member Workflow                      │
└─────────────────────────────────────────────────────────────┘

Person A (Alice)          Person B (Bob)           Person C (Carol)
     │                         │                         │
     ▼                         ▼                         ▼
┌─────────┐              ┌─────────┐              ┌─────────┐
│ Copy    │              │ Copy    │              │ Copy    │
│ Config  │              │ Config  │              │ Config  │
└────┬────┘              └────┬────┘              └────┬────┘
     │                         │                         │
     ▼                         ▼                         ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│ Edit 1 Line:    │      │ Edit 1 Line:    │      │ Edit 1 Line:    │
│ name: t5-large  │      │ name: Qwen2.5   │      │ name: gemma-7b  │
└────┬────────────┘      └────┬────────────┘      └────┬────────────┘
     │                         │                         │
     ▼                         ▼                         ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│ Same Training   │      │ Same Training   │      │ Same Training   │
│ Script:         │      │ Script:         │      │ Script:         │
│ bash train...   │      │ bash train...   │      │ bash train...   │
└────┬────────────┘      └────┬────────────┘      └────┬────────────┘
     │                         │                         │
     └─────────────┬───────────┴─────────────┬──────────┘
                   │                         │
                   ▼                         ▼
            ┌──────────────┐         ┌──────────────┐
            │ Auto-Detected│         │ All Results  │
            │ Architecture │         │ in WandB     │
            └──────────────┘         └──────────────┘
```

---

## 📝 What Each Person Changes

```yaml
┌────────────────────────────────────────────────────────────┐
│                  Alice's Config                             │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  model:                                                     │
│    name: "t5-large"              ← Only this line!         │
│    tokenizer: "t5-large"                                    │
│                                                             │
│  training:                                                  │
│    run_name: "alice_t5_large"    ← And this!               │
│                                                             │
│  [Everything else: SAME]                                    │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                   Bob's Config                              │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  model:                                                     │
│    name: "Qwen/Qwen2.5-7B-Instruct"  ← Different model     │
│    tokenizer: "Qwen/Qwen2.5-7B-Instruct"                    │
│    use_lora: true                    ← Enable LoRA         │
│                                                             │
│  training:                                                  │
│    run_name: "bob_qwen2.5"       ← Different name          │
│                                                             │
│  [Everything else: SAME]                                    │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                  Carol's Config                             │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  model:                                                     │
│    name: "google/gemma-7b"       ← Different model         │
│    tokenizer: "google/gemma-7b"                             │
│    use_lora: true                                           │
│    load_in_4bit: true            ← Enable quantization     │
│                                                             │
│  training:                                                  │
│    run_name: "carol_gemma"       ← Different name          │
│                                                             │
│  [Everything else: SAME]                                    │
└────────────────────────────────────────────────────────────┘
```

---

## 🚀 Training Flow (Same for Everyone!)

```
Start
  │
  ├─► bash train_multi_gpu.sh configs/your_config.yaml 4
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│  Step 1: Load Config (YAML)                              │
│  • Read model name from config                           │
│  • Read training parameters                              │
│  • Read data paths                                       │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  Step 2: Auto-Detect Architecture                        │
│  • Check model config from HuggingFace                   │
│  • Detect: Seq2Seq OR Causal LM                          │
│  • No manual specification needed!                       │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  Step 3: Load Model from HuggingFace                     │
│  • Download model (if not cached)                        │
│  • Apply LoRA (if enabled)                               │
│  • Apply quantization (if enabled)                       │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  Step 4: Setup Training                                  │
│  • Initialize optimizer                                  │
│  • Setup data loaders                                    │
│  • Configure multi-GPU (DDP)                             │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  Step 5: Train!                                          │
│  • Forward pass (handles both architectures)             │
│  • Backward pass                                         │
│  • Log to WandB                                          │
│  • Save checkpoints                                      │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
               Done! ✅
```

---

## 🎯 Model Auto-Detection (Behind the Scenes)

```
User sets: model.name = "any-huggingface-model"
                │
                ▼
        ┌───────────────┐
        │ Load Config   │
        │ from HF Hub   │
        └───────┬───────┘
                │
                ▼
     ┌──────────────────────┐
     │ Check Architecture   │
     │ is_encoder_decoder?  │
     └──────────┬───────────┘
                │
        ┌───────┴────────┐
        │                │
        ▼                ▼
   ┌─────────┐      ┌──────────┐
   │  TRUE   │      │  FALSE   │
   └────┬────┘      └────┬─────┘
        │                │
        ▼                ▼
  ┌──────────┐    ┌─────────────┐
  │ Seq2Seq  │    │ Causal LM   │
  │ (T5,BART)│    │ (Qwen,Gemma)│
  └────┬─────┘    └──────┬──────┘
       │                 │
       │                 │
       └────────┬────────┘
                ▼
        ┌───────────────┐
        │ Use Correct   │
        │ Forward Pass  │
        └───────────────┘
```

---

## 📊 Team Comparison Workflow

```
                    Week 1
                      │
    ┌─────────────────┼─────────────────┐
    │                 │                 │
    ▼                 ▼                 ▼
┌────────┐       ┌────────┐       ┌────────┐
│ Alice  │       │  Bob   │       │ Carol  │
│ T5     │       │ Qwen   │       │ Gemma  │
└───┬────┘       └───┬────┘       └───┬────┘
    │                │                │
    │    Train on Same Data          │
    │    Same Epochs                 │
    │    Same Seed (42)              │
    │                │                │
    └────────┬───────┴────────┬───────┘
             │                │
             ▼                ▼
      ┌────────────┐   ┌───────────┐
      │ WandB      │   │ Compare   │
      │ Tracking   │──►│ Results   │
      └────────────┘   └─────┬─────┘
                             │
                             ▼
                     ┌───────────────┐
                     │ Best Model:   │
                     │ Qwen2.5-7B    │
                     │ BLEU: 8.2     │
                     └───────────────┘
```

---

## ✅ Verification Checklist

```
Before Training:
┌─┐
│✓│ Config file created
└─┘
┌─┐
│✓│ Model name changed
└─┘
┌─┐
│✓│ Run name is unique
└─┘
┌─┐
│✓│ Test with: bash test_model_swap.sh configs/your_config.yaml
└─┘
┌─┐
│✓│ Ready to train!
└─┘
```

---

## 🔁 Iteration Cycle

```
Try Model 1               Results Not Good?
    │                           │
    ▼                           ▼
  Train ──────► Evaluate ──► Adjust Config
    │              │            │
    │              │            ▼
    │              │       Try Model 2
    │              │            │
    │              │            ▼
    │              │          Train
    │              │            │
    │              └────────────┘
    │              
    ▼
  Good Results! ✅
```

**Key Point:** Model swapping is just editing 1 line in config!

---

## 💡 The Beauty of This System

```
┌────────────────────────────────────────────────┐
│  Traditional Pipeline (❌ Bad)                  │
├────────────────────────────────────────────────┤
│                                                 │
│  Want to try new model?                        │
│  1. Edit model_factory.py                      │
│  2. Add new model class                        │
│  3. Update trainer.py                          │
│  4. Modify dataloader                          │
│  5. Test everything again                      │
│  6. Hope it doesn't break                      │
│                                                 │
│  Time: Hours to Days                           │
│  Risk: High                                    │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│  Our Pipeline (✅ Good)                         │
├────────────────────────────────────────────────┤
│                                                 │
│  Want to try new model?                        │
│  1. Edit config YAML (1 line)                  │
│  2. Run training script                        │
│                                                 │
│  Time: 2 Minutes                               │
│  Risk: Zero                                    │
└────────────────────────────────────────────────┘
```

---

## 🎓 Summary

**The Pipeline is Designed So:**

1. ✅ **Any team member** can swap models
2. ✅ **No code changes** needed
3. ✅ **Auto-detection** handles architecture differences
4. ✅ **Same training script** for all models
5. ✅ **WandB tracks** all experiments
6. ✅ **Git friendly** - just track config files
7. ✅ **Reproducible** - config = complete experiment spec

**Remember:** The model name in the config is ALL you need to change! 🎯
